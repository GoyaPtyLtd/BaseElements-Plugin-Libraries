This directory contains downloaded source archives.

**Source Archives:**
- Downloaded archives (`.tar.gz`, `.tar.xz` files) are stored here
- These are shared across all platforms
- Run `scripts/1_getSource.sh` to download all required archives

